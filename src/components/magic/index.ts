/*
 * Componentes portados desde librerías externas (Magic UI, etc.) al sistema de
 * diseño de Once UI: sin Tailwind, sin `cn()`, color exclusivamente por tokens.
 *
 * Antes de portar uno nuevo, revisa que Once UI no lo cubra ya —
 * ver docs/once-ui-vs-magic-ui.md.
 */
export { OrbitingCircles } from "./OrbitingCircles";
export type { OrbitingCirclesProps } from "./OrbitingCircles";
